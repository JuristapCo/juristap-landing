---
import { Image } from 'astro:assets';
import PatternLines from '../../assets/images/patterns/pattern-lines.webp';
import Character from '../../assets/images/brand/character-2.webp';
---

<section
  class="relative overflow-hidden py-16 md:py-20 lg:py-24 px-4 md:px-8 bg-primary text-white"
>
  <!-- Fondo con patrón -->
  <div class="absolute inset-0 z-0">
    <Image
      src={PatternLines}
      alt="Patrón de fondo"
      class="w-full h-full object-cover opacity-70"
      width={1920}
      height={1080}
      aria-hidden="true"
    />
  </div>

  <div
    class="relative z-10 max-w-7xl mx-auto flex flex-col-reverse md:flex-row md:items-center gap-8 lg:gap-12"
  >
    <!-- Personaje - En móvil estará abajo, en tablet y desktop a la izquierda -->
    <div
      class="w-full md:w-1/3 lg:w-1/4 flex justify-center md:justify-start mt-[-4rem] md:mt-0 md:mb-0 relative z-0"
    >
      <!-- Línea amarilla decorativa -->
      <div
        class="absolute top-[62%] left-[-100vw] right-0 h-1 md:h-0.5 bg-secondary transform -translate-y-1/2 -z-10"
      >
      </div>

      <Image
        src={Character}
        alt="Personaje legal"
        class="max-w-[200px] md:max-w-full h-auto relative"
        width={400}
        height={400}
      />
    </div>

    <!-- Pasos - En móvil tendrán un padding inferior para que el robot se sobreponga -->
    <div
      class="w-full md:w-2/3 lg:w-3/4 flex flex-col gap-12 md:gap-14 lg:gap-16 pb-20 md:pb-0 relative z-10"
    >
      <!-- Primer paso -->
      <div class="flex items-center relative">
        <!-- Círculo del icono con doble borde -->
        <div
          class="relative flex-shrink-0 w-[4.5rem] h-[4.5rem] md:w-20 md:h-20 lg:w-24 lg:h-24 rounded-full border-2 border-secondary flex items-center justify-center z-20"
        >
          <!-- Círculo exterior -->
          <div
            class="absolute -inset-1.5 lg:-inset-2 rounded-full border border-secondary opacity-70"
          >
          </div>

          <!-- Icono -->
          <svg
            class="w-8 h-8 md:w-9 md:h-9 lg:w-10 lg:h-10 fill-none stroke-secondary stroke-[1.5px] transition-transform duration-300 hover:scale-110"
          >
            <use href="/icons/flow-sprites.svg#icon-microphone"></use>
          </svg>

          <!-- Línea conectora -->
          <div
            class="absolute left-full top-1/2 w-6 md:w-8 lg:w-10 h-0.5 lg:h-0.5 bg-secondary opacity-80 transform -translate-y-1/2 z-10"
          >
          </div>
        </div>

        <!-- Contenido de texto -->
        <div class="pl-8 md:pl-10 lg:pl-12 z-20">
          <h3 class="text-xl md:text-2xl lg:text-3xl text-white font-semibold mb-3">
            Cuéntanos tu caso
          </h3>
          <p class="text-white/80 text-base lg:text-lg">
            Habla con nosotros con confianza, te escuchamos, sin juicios, solo queremos darte
            soluciones.
          </p>
        </div>
      </div>

      <!-- Segundo paso -->
      <div class="flex items-center relative">
        <!-- Círculo del icono con doble borde -->
        <div
          class="relative flex-shrink-0 w-[4.5rem] h-[4.5rem] md:w-20 md:h-20 lg:w-24 lg:h-24 rounded-full border-2 border-secondary flex items-center justify-center z-20"
        >
          <!-- Círculo exterior -->
          <div
            class="absolute -inset-1.5 lg:-inset-2 rounded-full border border-secondary opacity-70"
          >
          </div>

          <!-- Icono -->
          <svg
            class="w-8 h-8 md:w-9 md:h-9 lg:w-10 lg:h-10 fill-none stroke-secondary stroke-[1.5px] transition-transform duration-300 hover:scale-110"
          >
            <use href="/icons/flow-sprites.svg#icon-user"></use>
          </svg>

          <!-- Línea conectora -->
          <div
            class="absolute left-full top-1/2 w-6 md:w-8 lg:w-10 h-0.5 lg:h-0.5 bg-secondary opacity-80 transform -translate-y-1/2 z-10"
          >
          </div>
        </div>

        <!-- Contenido de texto -->
        <div class="pl-8 md:pl-10 lg:pl-12 z-20">
          <h3 class="text-xl md:text-2xl lg:text-3xl text-white font-semibold mb-3">
            Conecta con un abogado
          </h3>
          <p class="text-white/80 text-base lg:text-lg">
            Tu situación merece respuestas claras y rápidas. Uno de nuestros expertos estará ahí
            para ayudarte.
          </p>
        </div>
      </div>

      <!-- Tercer paso -->
      <div class="flex items-center relative">
        <!-- Círculo del icono con doble borde -->
        <div
          class="relative flex-shrink-0 w-[4.5rem] h-[4.5rem] md:w-20 md:h-20 lg:w-24 lg:h-24 rounded-full border-2 border-secondary flex items-center justify-center z-20"
        >
          <!-- Círculo exterior -->
          <div
            class="absolute -inset-1.5 lg:-inset-2 rounded-full border border-secondary opacity-70"
          >
          </div>

          <!-- Icono -->
          <svg
            class="w-8 h-8 md:w-9 md:h-9 lg:w-10 lg:h-10 fill-none stroke-secondary stroke-[1.5px] transition-transform duration-300 hover:scale-110"
          >
            <use href="/icons/flow-sprites.svg#icon-bulb"></use>
          </svg>

          <!-- Línea conectora -->
          <div
            class="absolute left-full top-1/2 w-6 md:w-8 lg:w-10 h-0.5 lg:h-0.5 bg-secondary opacity-80 transform -translate-y-1/2 z-10"
          >
          </div>
        </div>

        <!-- Contenido de texto -->
        <div class="pl-8 md:pl-10 lg:pl-12 z-20">
          <h3 class="text-xl md:text-2xl lg:text-3xl text-white font-semibold mb-3">
            Resuelve tus preocupaciones
          </h3>
          <p class="text-white/80 text-base lg:text-lg">
            Dile adiós a la incertidumbre. Estamos aquí para resolver lo que te quita el sueño,
            fácil y sin tanto enredo.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>
