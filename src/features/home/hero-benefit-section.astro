---
import SectionContainer from '../../components/ui/section-container.astro';
import { Image } from 'astro:assets';
import PatternHero from '../../assets/images/patterns/pattern-hero.webp';
import InterfaceIcon from '../../assets/images/interface.webp';
import LockIcon from '../../assets/images/lock.webp';
import StatusIcon from '../../assets/images/status.webp';
import QuoteAndCharacter from '../../assets/images/brand/quote-and-character.webp';
---

<SectionContainer
  background="bg-primary text-white"
  withSpacing={false}
  constrained={false}
  py="py-0"
  aria-label="Sección de beneficios"
>
  <!-- Base background with pattern -->
  <div
    class="absolute inset-0 w-full h-full pointer-events-none overflow-hidden"
    aria-hidden="true"
  >
    <Image
      src={PatternHero}
      alt="Patrón de fondo decorativo"
      class="absolute inset-0 w-full h-full object-cover object-center opacity-20 mix-blend-overlay"
      width={1920}
      height={1080}
    />
  </div>

  <!-- Gradient overlay -->
  <div
    class="absolute inset-0 w-full h-full pointer-events-none overflow-hidden"
    style="background:radial-gradient(circle at 50% 50%, #896D9A 0%, #4F0786 55%, #3C007D 100%); opacity:0.85;"
    aria-hidden="true"
    role="presentation"
  >
  </div>

  <!-- Decorative elements -->
  <div
    class="absolute top-0 right-0 w-64 h-64 bg-purple-800/20 rounded-full blur-3xl transform translate-x-1/2 -translate-y-1/2 pointer-events-none"
    aria-hidden="true"
    role="presentation"
  >
  </div>
  <div
    class="absolute bottom-0 left-0 w-96 h-96 bg-purple-700/20 rounded-full blur-3xl transform -translate-x-1/2 translate-y-1/4 pointer-events-none"
    aria-hidden="true"
    role="presentation"
  >
  </div>

  <!-- Subtle grid pattern -->
  <div
    class="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.05)_1px,transparent_1px),linear-gradient(to_right,rgba(255,255,255,0.05)_1px,transparent_1px)] bg-[size:80px_80px] opacity-10 pointer-events-none"
    aria-hidden="true"
    role="presentation"
  >
  </div>

  <div
    class="mx-auto px-4 sm:px-6 md:px-8 lg:px-12 xl:px-16 py-12 sm:py-16 md:py-20 lg:py-24 xl:py-28 relative z-10"
  >
    <header class="text-center mb-12 sm:mb-14 md:mb-10 lg:mb-10 xl:mb-16 relative">
      <h2
        class="text-2xl sm:text-3xl md:text-4xl font-bold mb-1 sm:mb-1 md:mb-2 lg:mb-3 tracking-tight"
      >
        Simplifica tus procesos legales
      </h2>
      <p
        class="text-sm sm:text-base md:text-lg text-white/90 max-w-[300px] sm:max-w-[400px] md:max-w-[500px] lg:max-w-[600px] xl:max-w-[800px] mx-auto leading-relaxed"
      >
        Descubre cómo nuestra plataforma transforma la manera de gestionar tus casos legales
      </p>
    </header>

    <div
      class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 sm:gap-8 md:gap-10 xl:gap-12 text-center mb-8 md:mb-12 lg:mb-16 items-stretch"
      role="list"
      aria-label="Beneficios principales"
    >
      <article
        class="flex flex-col items-center gap-4 sm:gap-5 md:gap-6 px-2 sm:px-4 rounded-2xl py-8 h-full focus-within:ring-2 focus-within:ring-secondary outline-none order-2 lg:order-1"
        role="listitem"
        tabindex="0"
        aria-label="Plataforma fácil e intuitiva"
      >
        <div
          class="relative w-[180px] sm:w-[160px] md:w-[250px] md:h-[210px] lg:w-[280px] lg:h-[210px] flex items-center justify-center"
        >
          <Image
            src={InterfaceIcon}
            alt="Interfaz intuitiva de la plataforma"
            class="w-full h-auto object-contain"
          />
        </div>
        <h3 class="text-xl sm:text-2xl xl:text-3xl font-bold text-white mb-1">
          Plataforma <br />
          <span class="text-secondary">fácil e intuitiva</span>
        </h3>
        <p class="text-base sm:text-lg text-white/80 max-w-xs mx-auto">
          Navega fácilmente por nuestra plataforma diseñada para simplificar tus procesos legales.
        </p>
      </article>

      <article
        class="flex flex-col items-center gap-4 sm:gap-5 md:gap-6 px-2 sm:px-4 rounded-2xl py-8 h-full focus-within:ring-2 focus-within:ring-secondary outline-none md:col-span-2 lg:col-span-1 order-1 lg:order-2"
        role="listitem"
        tabindex="0"
        aria-label="Procesos 100% confidenciales"
      >
        <div class="relative w-full max-w-[120px] sm:max-w-[160px] md:max-w-[180px] mx-auto">
          <Image
            src={LockIcon}
            alt="Procesos 100% confidenciales"
            class="w-full h-auto object-contain"
          />
        </div>
        <h3 class="text-xl sm:text-2xl xl:text-3xl font-bold text-white mb-1">
          Procesos 100% <br /><span class="text-secondary">confidenciales</span>
        </h3>
        <p class="text-base sm:text-lg text-white/80 max-w-xs mx-auto">
          Tu información está protegida con los más altos estándares de seguridad y privacidad.
        </p>
      </article>

      <article
        class="flex flex-col items-center gap-4 sm:gap-5 md:gap-6 px-2 sm:px-4 rounded-2xl py-8 h-full focus-within:ring-2 focus-within:ring-secondary outline-none order-3 lg:order-3"
        role="listitem"
        tabindex="0"
        aria-label="Sigue tu caso en tiempo real"
      >
        <div
          class="relative w-[180px] sm:w-[160px] md:w-[250px] md:h-[210px] lg:w-[280px] lg:h-[210px] flex items-center justify-center"
        >
          <Image
            src={StatusIcon}
            alt="Sigue tu caso en tiempo real"
            class="w-full h-auto object-contain"
          />
        </div>
        <h3 class="text-xl sm:text-2xl xl:text-3xl font-bold text-white mb-1">
          Sigue tu caso <br /><span class="text-secondary">en tiempo real</span>
        </h3>
        <p class="text-base sm:text-lg text-white/80 max-w-xs mx-auto">
          Actualizaciones instantáneas y seguimiento detallado de cada etapa de tu proceso.
        </p>
      </article>
    </div>

    <div class="flex justify-center mt-4 mb-4">
      <a
        href="#registrate"
        class="px-8 py-3 bg-secondary text-primary text-lg font-medium rounded-xl shadow-lg hover:bg-secondary-400 transition-colors duration-300 focus:outline-none focus:ring-2 focus:ring-secondary/50 focus:ring-offset-2 focus:ring-offset-primary"
      >
        Registrate ahora
      </a>
    </div>
    <div class="flex justify-center mb-2">
      <p class="text-white/80 text-sm text-center max-w-xs">
        Se acerca la oportunidad de reencontrar tu tranquilidad
      </p>
    </div>

    <div class="flex justify-center items-center mt-4 sm:mt-6 md:mt-8 lg:mt-10 xl:mt-12">
      <Image
        src={QuoteAndCharacter}
        alt="Cita y personaje"
        class="w-full max-w-[400px] sm:max-w-[500px] md:max-w-[600px] lg:max-w-[700px] xl:max-w-[800px] object-contain"
      />
    </div>
    <div class="flex justify-center mt-2 mb-2">
      <p class="text-white/90 text-base sm:text-lg text-center leading-relaxed max-w-xl">
        Es hora de embarcar, viajero<br />
        <span class="font-bold">Estamos aquí para guiarte en tu trayecto legal.</span>
      </p>
    </div>
  </div>

  <style>
    .bg-primary {
      background: radial-gradient(circle at 50% 40%, #896d9a 0%, #4f0786 55%, #3c007d 100%);
    }
  </style>
</SectionContainer>
